import clsx from "clsx";

export default function CoinHeader({ coin, isWatchlisted = false, onToggleWatchlist }) {
  const change = coin?.market_data?.price_change_percentage_24h;
  const price = coin?.market_data?.current_price?.usd;
  return <header className="flex flex-col gap-5 border-b border-[var(--color-border)] pb-6 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-[var(--color-accent-muted)] text-lg font-semibold text-[var(--color-accent)]">{coin?.symbol?.slice(0, 3).toUpperCase() ?? "?"}</div><div><div className="flex items-center gap-2"><h1 className="text-2xl font-semibold text-[var(--color-text-primary)]">{coin?.name ?? "Coin"}</h1><span className="text-sm uppercase text-[var(--color-text-muted)]">{coin?.symbol}</span></div><p className="mt-1 font-tabular text-xl text-[var(--color-text-primary)]">{price == null ? "-" : new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", maximumFractionDigits: price < 1 ? 6 : 2 }).format(price)} <span className={clsx("ml-2 text-sm", change > 0 ? "text-[var(--color-up)]" : change < 0 ? "text-[var(--color-down)]" : "text-[var(--color-text-muted)]")}>{change == null ? "" : `${change > 0 ? "+" : ""}${change.toFixed(2)}%`}</span></p></div></div><button type="button" onClick={() => onToggleWatchlist?.(coin)} aria-pressed={isWatchlisted} className="rounded-[var(--radius-md)] border border-[var(--color-border)] px-3 py-2 text-sm font-medium text-[var(--color-text-primary)] hover:bg-[var(--color-surface-hover)]">{isWatchlisted ? "★ In watchlist" : "☆ Add to watchlist"}</button></header>;
}